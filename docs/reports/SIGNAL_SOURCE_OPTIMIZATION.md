# 信号源管理优化分析

## 当前问题诊断

### 1. 自定义信息源痛点
- **UI 简陋**：仅显示名称+地区+删除按钮，缺少详细信息
- **功能缺失**：无编辑、无批量操作、无排序
- **验证不持久**：验证结果即时显示后消失，无法查看历史
- **无元数据**：无法添加标签、备注、分类
- **无状态监控**：无健康度、响应时间、更新时间
- **数据管理弱**：无导入导出、无备份恢复

### 2. 内置信息源痛点
- **交互不直观**：checkbox 标签式，难以看清源状态
- **信息不完整**：无 URL、描述、验证详情
- **无分组**：所有源平铺，难以管理
- **搜索弱**：DOM 操作不稳定
- **验证结果简陋**：仅显示状态名称，无详细信息
- **无法编辑**：内置源无法添加元数据

### 3. 整体架构痛点
- **状态分散**：customSources、disabledSources、sourceHealth 独立存储
- **无监控机制**：无法自动检测源健康度
- **无操作反馈**：缺少 toast 提示、加载状态不明确
- **无优先级设置**：无法设置源的使用优先级
- **无源分类**：难以按类别/标签筛选

## 优化方案（按用户选择）

### 需求确认
- ✅ 分开区域 + 卡片优化
- ✅ 完整管理套件（编辑、批量、排序、导入导出）
- ✅ 自动监控 + 提醒（响应时间、健康度）
- ✅ 界面优化、操作增强、验证监控、数据管理、高级管理（全选）

### 实施计划

#### 阶段 1：界面优化
- 卡片化布局（统一视觉设计）
- 显示源详细信息（URL、标签、备注）
- 实时状态指示（健康度、验证状态）
- 清晰的操作按钮（编辑、验证、删除）

#### 阶段 2：操作增强
- 编辑功能（修改名称、URL、标签、备注）
- 批量操作（批量启用/禁用/删除）
- 拖拽排序
- 复制 URL 功能
- Toast 提示反馈

#### 阶段 3：验证监控
- 响应时间显示
- 健康度指标（基于响应时间和失败率）
- 自动定期验证
- 异常提醒

#### 阶段 4：高级管理
- 按地区分组展示
- 高级搜索（多条件筛选）
- 标签分类系统
- 优先级设置

#### 阶段 5：数据管理
- 导入配置（JSON/YAML）
- 导出配置（备份）
- 一键恢复默认配置

## 数据结构设计

### 扩展的源数据结构
```javascript
{
  id: number,
  name: string,
  url: string,
  region: 'domestic' | 'overseas' | 'global',
  category: string,  // 分类：AI、硬件、开源、科研...
  tags: string[],  // 标签数组
  notes: string,  // 备注
  health: {
    status: 'healthy' | 'warning' | 'error' | 'unknown',
    lastCheck: timestamp,
    responseTime: number,
    failCount: number,
    itemCount: number
  },
  priority: number,  // 优先级 1-5
  isCustom: boolean
}
```

## 关键功能点

### 1. 卡片组件
- 统一的卡片设计（自定义源和内置源）
- 显示健康度图标
- 验证状态徽章
- 拖拽手柄

### 2. 批量操作栏
- 全选/反选
- 批量启用/禁用
- 批量删除
- 导出选中源

### 3. 高级搜索
- 名称搜索
- URL 搜索
- 地区筛选
- 分类筛选
- 标签筛选
- 健康度筛选

### 4. 自动监控
- 定期验证（可配置间隔）
- 健康度计算算法
- 异常阈值触发提醒
- 验证历史记录

### 5. 导入导出
- JSON 格式导出所有源配置
- 支持导入自定义源
- 备份/恢复功能
- 配置模板（预设配置包）